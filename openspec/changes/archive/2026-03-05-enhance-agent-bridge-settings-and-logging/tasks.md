## 1. 配置模型与持久化基础

- [x] 1.1 梳理 Agent Bridage 现有连接参数来源，新增端点配置模型（IP/Port）与默认值定义
- [x] 1.2 实现 IP/Port 合法性校验逻辑（含端口范围与空值处理）并输出可识别错误
- [x] 1.3 接入本地持久化读写，在启动时加载配置并在无效场景回退默认值

## 2. 运行时接入与连接行为

- [x] 2.1 在 Agent Bridage 初始化流程中注入配置提供者，替换硬编码端点读取方式
- [x] 2.2 实现 `SetEndpoint(ip, port)`（或等价接口），在配置变更后触发保存与连接参数刷新
- [x] 2.3 完成配置变更后的重连流程（先断开再连接），并避免无变化配置触发重复重连

## 3. 日志增强

- [x] 3.1 使用 `IXLogger`/`IXLogManager` 为连接生命周期关键事件增加结构化日志
- [x] 3.2 为消息发送、接收与消息处理异常路径增加日志，并统一上下文字段（ip:port、方向、状态、异常摘要）
- [x] 3.3 依据事件类型校准日志级别（info/warn/error），控制高频路径的日志噪声

## 4. 测试与回归验证

- [x] 4.1 新增/更新 EditMode 测试：覆盖有效保存、非法拒绝、重启加载、无效配置回退
- [x] 4.2 新增/更新日志相关测试：覆盖连接成功、连接失败、消息异常等关键日志触发
- [x] 4.3 执行受影响测试集并记录结果，确认 Agent Bridage 变更不破坏现有行为
